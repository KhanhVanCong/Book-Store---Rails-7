<p style="color: green"><%= notice %></p>

<div class="row">
  <div class="col-3">
    <h1>Tags</h1>
  </div>
  <div class="col-9 text-right">
    <%= link_to "New tag", new_cms_tag_path, class: "btn btn-primary" %>
  </div>
</div>

<div class="row">
  <div class="col-12">
    <div id="cms_tags">
      <table class="table table-striped">
        <thead>
        <tr>
          <th>#</th>
          <th>Name</th>
          <th>Book count</th>
          <th>Action</th>
        </tr>
        </thead>
        <tbody>
        <% @tags.each_with_index do |tag, index| %>
          <%= render partial: "cms/tags/tag", locals: { tag: tag, index: index } %>
        <% end %>
        </tbody>
      </table>
      <div class="float-right">
        <%== pagy_bootstrap_nav(@pagy) %>
      </div>
    </div>
  </div>
</div>
